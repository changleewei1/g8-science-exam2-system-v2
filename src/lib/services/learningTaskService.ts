import type { SubjectServiceContext } from "@/lib/services/subject-context";

export type LearningTaskContext = SubjectServiceContext;

export function assertLearningTaskContext(_ctx: LearningTaskContext): void {
  // 預留：與 learning_tasks、任務進度 API 對齊
}
